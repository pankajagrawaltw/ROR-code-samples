<%= form_for @comment,  url: post_comment_path(comment.id), method: :patch do |f| %>
	<div class="field">
	  <%= f.label :comment %><br />
	  <%= f.text_field :comment, autofocus: true %>
	</div>

	<div class="actions">
	  <%= f.submit "Submit" %>
	</div>
<% end %>
